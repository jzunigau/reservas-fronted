import React from 'react';

const ProtectedRoute = ({ children }) => {
  // Aquí debería ir la lógica de autenticación real
  // Por ahora, solo renderiza los hijos
  return children;
};

export default ProtectedRoute; 